apiVersion: v1
kind: Secret
metadata:
  name: admin-api-secret
type: Opaque
stringData:
  MYSQL_USER: "${MYSQL_USER}"
  MYSQL_PASSWORD: "${MYSQL_PASSWORD}"
  # prod 는 평문 ADMIN_PASSWORD 를 쓰지 않는다. sha256 해시만 넣는다.
  ADMIN_PASSWORD_SHA256: "${ADMIN_PASSWORD_SHA256}"
  ADMIN_TOKEN_SECRET: "${ADMIN_TOKEN_SECRET}"
