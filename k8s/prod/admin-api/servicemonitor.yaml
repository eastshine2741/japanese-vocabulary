apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: admin-api
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: admin-api
  namespaceSelector:
    matchNames:
      - kotonoha
  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 30s
      scrapeTimeout: 10s
